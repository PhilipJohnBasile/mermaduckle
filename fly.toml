app = "mermaduckle"

[build]
  # Use the provided Dockerfile for Fly builds
  dockerfile = "deploy/Dockerfile.fly"

[[services]]
  internal_port = 3001
  protocol = "tcp"

  [[services.ports]]
    handlers = ["http"]
    port = 80

  [[services.ports]]
    handlers = ["tls", "http"]
    port = 443
