app = "mermaduckle"

[build]
  # Use the provided Dockerfile for Fly builds
  dockerfile = "deploy/Dockerfile.fly"

[mounts]
  source = "mermaduckle_data"
  destination = "/app/data"

[env]
  DATABASE_PATH = "/app/data/app.db"

[[services]]
  internal_port = 3001
  protocol = "tcp"

  [[services.ports]]
    handlers = ["http"]
    port = 80

  [[services.ports]]
    handlers = ["tls", "http"]
    port = 443

  [[services.http_checks]]
    grace_period = "10s"
    interval = "15s"
    method = "get"
    path = "/api/health"
    protocol = "http"
    timeout = "5s"
